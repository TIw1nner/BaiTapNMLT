#include <iostream>

using namespace std;

int main(){
    int a,b,c;
    cout << "Hay nhap vao he so a, b, c: ";
    cin >> a >> b >> c;
    cout << a << "X^2 + " << b << "X + " << c << " = 0";
    return 0;
}